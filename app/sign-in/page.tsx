import LoginForm from "@/components/form/LoginForm";

export default function Login() {
  return (
    <main
      className={
        "w-screen h-screen flex items-center justify-center px-2.5 sm:px-0"
      }
    >
      <LoginForm />
    </main>
  );
}
